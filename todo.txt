TODO LIST FOR VOID NOTES CUSTOM INSTALLER FIX
=============================================

PROBLEMS ENCOUNTERED:
1. Batch file execution issue:
   - Symptom: "build-custom-installer.bat: command not found" when running in bash/MSYS
   - Root cause: The .bat file needs to be executed in Windows CMD, not bash
   - Fix: Use `cmd /c "build-custom-installer.bat"` or run directly in CMD/PowerShell

2. TypeScript compilation issue:
   - Symptom: "tsc: command not found" when trying to compile Electron main process
   - Root cause: TypeScript is installed as dev dependency, not globally
   - Fix: Use `npx tsc -p tsconfig.electron.json` instead of `tsc`

3. Build warnings (non-blocking but should be addressed):
   - CSS syntax warnings in dist/assets/index-DTFto7VU.css:
     * Expected identifier but found whitespace at line 588:8
     * Unexpected "100%" at line 588:9
   - Related to: `width: 100%;` CSS rule
   - Suggestion: Check CSS files for missing semicolons or syntax errors

4. Electron builder interruption:
   - Symptom: Process interrupted with exit code 130 (likely Ctrl+C)
   - Status: Build was running but got stopped before completion
   - Next step: Let the electron-builder process run to completion

5. Custom installer verification needed:
   - Need to verify that the custom NSIS installer (VoidNotesInstaller.nsi) 
     correctly packages all required files:
     * dist/**/*
     * dist-electron/**/*  
     * package.json
   - Verify installer creates proper shortcuts and registry entries

ACTION ITEMS FOR TOMORROW:
- [ ] Run full build: `npm run build` (using npx tsc for TypeScript steps)
- [ ] Address CSS warnings if they cause issues
- [ ] Run electron-builder to completion: `npx electron-builder --win --publish never`
- [ ] Test the custom installer build script: `cmd /c "build-custom-installer.bat"`
- [ ] Verify output installer exists and works: release\\Void-Notes-Setup-0.4.1-Custom.exe
- [ ] Consider cleaning up dist/ and dist-electron/ before builds if needed

NOTES:
- The project uses electron-builder with NSIS target
- Custom installer script is at resources\\installer\\VoidNotesInstaller.nsi
- Version is 0.4.1 (from package.json)
- Product name: Void Notes